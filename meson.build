project('ti', 'c',
  version : '0.1',
  default_options : ['warning_level=3'])

ncurses = dependency('ncurses')

executable('ti',
           'ti.c',
           install : false,
           dependencies: ncurses)